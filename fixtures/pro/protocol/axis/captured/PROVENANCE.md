# Captured fixtures — AXI-Stream

This directory holds traces acquired or produced from public open-source
projects. They complement the deterministic synthetic fixtures next door in
`../generated/` by exercising the decoder against real-world bus traffic.

## License allow-list

Captured fixtures must come from projects under one of these licenses:

- MIT
- BSD-2-Clause / BSD-3-Clause
- Apache-2.0
- ISC
- CC0-1.0
- public domain

GPL/AGPL/proprietary captures are excluded by policy. The static test
`test/static/captured_fixture_licenses_test.dart` walks every
`PROVENANCE.md` in the corpus and fails the build if any entry uses a
license outside this list, or if any fixture file in this directory has no
matching entry.

## Per-fixture entries

Each fixture committed below carries a block in this file with: source
project, source commit SHA, license + SPDX, author/maintainer, original
location of the testbench inside the source repo, capture method (direct
file from repo, or the exact `iverilog` / `verilator` / `cocotb`
invocation used locally), acquisition date, file size, scenario summary,
and 2–3 hand-verified anchor transactions confirmed by eye before the
snapshot was committed.

### Block template

```markdown
## `<fixture_name>.fst` — <one-line scenario summary>

- **Source project:** <project name> — <https://github.com/...>
- **Source commit:** `<sha>` (acquired YYYY-MM-DD)
- **License:** <name> (SPDX: `<identifier>`)
- **Author / maintainer:** <name>
- **Original location:** `<path/to/testbench>`
- **Capture method:** <"direct .fst from repo" | "built locally with `iverilog …`" | "built locally with `verilator --trace-fst …`" | "cocotb run: `pytest tb/… `">
- **Acquired:** YYYY-MM-DD
- **Size:** <bytes raw / bytes committed>
- **What it exercises:** <2–3 sentence scenario description>
- **Hand-verified anchors:**
  - <e.g. "write to 0x40, data 0xdeadbeef, at t≈230 ns">
  - <e.g. "burst of 4 reads starting at 0x1000">
- **Snapshot regenerated by:** `dart run tool/regenerate_captured_snapshots.dart --decoder axis --fixture <fixture_name>`
```

## Current captures

## `forencich_axis_register.fst` — 9 cocotb sub-tests against alexforencich/verilog-axis axis_register

- **Source project:** Alex Forencich / verilog-axis — <https://github.com/alexforencich/verilog-axis>
- **Source commit:** `48ff7a7e2ef782cf778d47910cf85835c64b1bce` (acquired 2026-05-31)
- **License:** MIT (SPDX: `MIT`) — per-file header in `COPYING`, `tb/axis_register/Makefile`, and `tb/axis_register/test_axis_register.py`.
- **Author / maintainer:** Alex Forencich
- **Original location:** `rtl/axis_register.v` + `tb/axis_register/test_axis_register.py`.
- **Capture method:** cocotb run from a clean clone — drop in
  `helpers/forencich/iverilog_dump.v` (adds an `aresetn = !axis_register.rst`
  net to the FST dump scope), then
  `cd verilog-axis/tb/axis_register && PATH=~/.venvs/wavecrux-cocotb/bin:$PATH make WAVES=1 SIM=icarus`.
  Shares the AXI4-Full one-time setup
  (`test/fixtures/protocol/axi4_full/captured/helpers/cocotb_setup.md`):
  Python 3.13 venv + cocotb 2.0.1 + cocotb-bus + cocotbext-axi.
- **Acquired:** 2026-05-31
- **Size:** 10 847 bytes committed FST / 278 KB snapshot JSON (789 transactions).
- **What it exercises:** All 9 cocotb sub-tests in `test_axis_register.py`
  run back-to-back across ~226 µs of simulated time: `run_test_001` (single
  beat), `run_test_002/003/004` (packet-length sweep 1..16 bytes with
  per-beat tid/tdest walking), `run_test_tuser_assert_001` (AMBA-spec
  packet-error convention via `tuser`), and four `run_stress_test_*` runs
  that pseudo-randomly interleave back-to-back, tready-paced, and
  tvalid-paced patterns. Covers the AXIS handshake, tlast packet
  boundaries, single-beat packets, tdest/tid sideband aggregation, and
  packet-length variation.
- **Hand-verified anchors:**
  - First packet at `startTime=60000 ps` (= 60 ns): `AXIS packet (1 beats, 1 bytes)`,
    `payload_bytes="00"`, `tdest_routing="1"`, `tid_stream="1"` — matches the
    cocotb log's first `RX frame: AxiStreamFrame(tdata=bytearray(b'\x00'),
    tkeep=[1], tid=[1], tdest=[1], ..., sim_time_start=60008)`.
  - Last packet at `startTime=225990008 ps`: `AXIS packet (7 beats, 7 bytes)`,
    `payload_bytes="00010203040506"`, `tdest_routing="128"`, `tid_stream="128"` —
    matches the final cocotb stress run's terminator with `tid=128`.
  - Transaction count = 789, exactly the number of S-AXIS frames the
    cocotb log reports across all 9 sub-tests.
- **Determinism note:** `test_axis_register.py` uses `random` without an
  explicit seed, so per-beat tid/tdest/data sequences differ slightly
  between fresh cocotb runs. The committed FST is a one-time frozen
  capture; only the snapshot is regenerated when the decoder output
  format changes.
- **Snapshot regenerated by:** `REGENERATE=1 flutter test test/services/decoders/axis_captured_fixtures_test.dart`
